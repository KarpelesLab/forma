[package]
name = "gpudemo"
description = "Forma example: render a frame on the CPU, then route it through the GPU (forma-gpu) and dump the read-back RGBA."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
forma.workspace = true
forma-gpu.workspace = true

[lints]
workspace = true
