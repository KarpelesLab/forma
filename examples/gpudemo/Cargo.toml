[package]
name = "gpudemo"
description = "Stipple example: render a frame on the CPU, then route it through the GPU (stipple-gpu) and dump the read-back RGBA."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
stipple.workspace = true
stipple-gpu.workspace = true

[lints]
workspace = true
